<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.edu.nbl.housekeeper">

    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.KILL_BACKGROUND_PROCESSES" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".main.MainActivity" />
        <activity android:name=".main.LeadActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".main.LogoActivity" />

        <service android:name=".service.MusicService" />

        <activity android:name=".main.AboutActivity" />
        <activity android:name=".main.SettingActivity" />
        <activity android:name=".main.SpeedActivity" />
        <activity android:name=".main.SoftmgrActivity" />
        <activity android:name=".main.PhonemgrActivity" />
        <activity android:name=".main.TelmgrActivity" />
        <activity android:name=".main.FilemgrActivity" />
        <activity android:name=".main.SdcleanActivity" />
        <activity android:name=".main.TelmgrshowActivity" />
        <activity android:name=".main.SoftmgrShowActivity" />
        <activity android:name=".main.FilemgrShowActivity"></activity>
    </application>

</manifest>